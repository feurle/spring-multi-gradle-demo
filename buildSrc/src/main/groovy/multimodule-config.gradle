plugins {
    id 'java'
    id 'checkstyle'
}

sourceCompatibility = JavaVersion.VERSION_17
targetCompatibility = JavaVersion.VERSION_17

repositories {
    mavenCentral()
}

dependencies {
   // common dependencies
}

tasks.named('test') {
    useJUnitPlatform()
}